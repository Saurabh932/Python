# a = 1
# b = 2
#
# print("a & b: ", a&b)   # Bitwise AND operator Returns 1 if both the bits are 1 else 0.
# print("a | b: ", a|b)   # Bitwise OR operator Returns 1 if either of the bit is 1 else 0.
# print("~a: ", ~a)   # Bitwise not operator: Returns one’s complement of the number.
#
# '''  Note: 1. Whenever we do XOR of small digit and large digit the answer is addition of them:
#                     (small digit) XOR (large digit) = sum(SD, LD)
#            2. Whenever we do XOR of large digit and small digit the answer is subtraction of them:
#                     (large digit) XOR (small digit) = sub(SD, LD)
# '''
# # Bitwise xor operator: Returns 1 if one of the bits is 1 and the other is 0 else returns false.
# print("a ^ b (it is subtracted): ", a^b)
# print("b ^ a (it is added): ", b^a)
#
#
# # Python program to show shift operators
#
# a = 10
# b = -10
#
# # print bitwise right shift operator
# '''  Note: whenever we do right shift of a number it gets half.  '''
# print("a >> 1 =", a >> 1)
# print("b >> 1 =", b >> 1)
#
# a = 15
# b = -30
#
# # print bitwise left shift operator
# '''  Note: whenever we do left shift of a number it gets double.  '''
# print("a << 1 =", a << 1)
# print("b << 1 =", b << 1)


